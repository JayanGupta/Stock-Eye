import { ComingSoon } from "@/components/common/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Workspace configuration, roles, and billing."
      feature="Workspace settings are next up"
    />
  );
}
